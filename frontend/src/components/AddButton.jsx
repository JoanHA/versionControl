import React from "react";

function AddButton({param}) {
  return (
    <div>
      <button className="btn btn-secondary  medium-rounded-right">+</button>
    </div>
  );
}

export default AddButton;
